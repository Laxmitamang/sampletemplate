import React from 'react';

const Header = () => {
  const headerStyle = {
    backgroundColor: 'black',
    color: 'white'
  };
  return <header style={headerStyle}>
      <h1>apple</h1>
  </header>;
};

export default Header;
